import {render,screen} from "@testing-library/react";import {MemoryRouter} from "react-router-dom";import {describe,it,expect} from "vitest";import App from "../App.jsx";describe("App",()=>{it("renders shell",()=>{render(<MemoryRouter><App/></MemoryRouter>);expect(screen.getByText("RoboOps")).toBeInTheDocument();expect(screen.getByRole("heading",{name:"Dashboard"})).toBeInTheDocument()})});
